【确定的实现技术栈】
- TypeScript 5.9
- React 19
- @opentui/core 0.5.1
- @opentui/react 0.5.1
- Bun 构建

设计时需要同时考虑：
1. 产品体验是否成立
2. React + OpenTUI 是否能实现
3. 组件是否适合沉淀为可复用基础组件
4. 宽屏、窄屏、SSH 和低能力终端是否可用

在设计页面前，先区分：
- 应沉淀为全局基础能力的内容
- 应沉淀为通用组件的内容
- 仅属于当前业务页面的内容

## 仓库定位

- 本仓库只负责 UI 研究、设计、评审与正式交付，不保存真实产品的业务代码和实现测试。
- 真实产品的实现仓库独立存在，本仓库不假设其路径或内部目录结构。
- 所有正式设计必须在已确定的技术栈和终端能力范围内可实现，不能把实现不了的视觉效果留给后续阶段自行解释。
- 设计与已知技术约束发生冲突时，记录冲突证据、影响范围和待裁决问题；在完成裁决前保持 `draft`，不得静默偏离或创造第三套规则。

## 正式 UI 交付结构

正式 UI 交付固定为两层：一份全局规范和一个页面详情目录。

```text
kunpeng-devkit-ai-tui/
├── UI-SPEC.md              # 全局 UI 规范，跨页面规则的唯一事实源
└── pages/
    ├── _TEMPLATE.md        # 页面详情模板，不代表真实页面
    └── <page-id>.md        # 每个页面一份完整设计合同
```

- `docs/`、`web/` 和 `assets/` 可以继续保存研究、推导、竞品、演示和评审材料，但不属于正式交付入口。
- `UI-SPEC.md` 与 `pages/` 是正式规范性产物。研究材料与正式交付不一致时，以已批准的正式交付为准，并及时修正偏差。
- 页面相关的字符帧、状态、交互、示例数据和验收条件必须合并在同一个 `pages/<page-id>.md` 中，避免后续实现从多个零散文件自行拼接语义。
- 图片或其他大型参考资源可以继续放在 `assets/`，由页面详情使用相对路径引用；它们只能辅助理解，不能承载规范性要求，也不能补充页面文件中缺失的行为、状态或验收条件。

## 全局 UI 规范

`UI-SPEC.md` 至少包含：

- 文档状态、`ui-spec-version`、更新时间和适用产品范围。
- 产品级体验原则、信息层级和全局 Shell 结构。
- 语义 token：颜色、前景、表面、边框、间距、字符和数据色阶。
- 排版、对齐、截断、CJK/emoji/Braille display width 规则。
- 全局布局、宽窄屏断点、resize、滚动和溢出规则。
- 通用组件的语义、状态、变体和组合边界；只描述可验证的行为契约，不绑定具体实现文件。
- 全局焦点模型、键盘语义、鼠标等价路径、keybar 和高风险确认规则。
- `loading / empty / error / disabled / selected / focused` 等公共状态。
- SSH、TrueColor、16 色、`NO_COLOR`、Braille 缺失和纯键盘降级策略。
- 动效、性能、可访问性和禁止项。
- 规则 ID、冲突处理和版本精确匹配策略。

跨页面生效的规则只能写入 `UI-SPEC.md`，页面文件应引用对应规则 ID，不得复制后形成页面私有版本。修改任何全局规则时，必须提升 `ui-spec-version`，列出受影响页面，并由人工审核后将文档状态标记为 `approved`。

版本和例外采用确定规则：

- 页面 `ui-spec-version` 必须与 `UI-SPEC.md` 的版本字符串完全相等；不使用版本范围，不把兼容性判断留到后续阶段。
- 全局版本变化后，必须检查所有页面、更新其版本声明，并将受影响页面重新标记为 `draft` 直至人工复审；确认不受影响的页面可以只更新版本声明并记录检查结果。
- 页面不得自行覆盖全局规则。确需例外时，先在 `UI-SPEC.md` 的例外登记表中创建 `EXC-*`，写明页面、被覆盖规则、理由、范围、审批状态和退出条件；页面文件只能引用状态为 `approved` 的例外 ID。

## 页面详情文件

每个 `pages/<page-id>.md` 必须是该页面可独立使用的完整设计合同，并在开头声明：

```yaml
page-id: <stable-id>
title: <page-title>
status: draft | approved | deprecated
ui-spec-version: <exact-global-version>
updated-at: YYYY-MM-DD
```

正文至少包含：

- 页面目标、用户任务、范围以及明确不包含的内容。
- 入口、退出、前置条件及与其他页面的关系。
- 区域结构、信息层级和使用的全局组件/规则 ID。
- 160×50 宽屏和 58×32 窄屏字符帧，直接嵌入 Markdown 代码块。
- 数据字段、示例数据、格式化、排序、截断和空值规则。
- `default / loading / empty / error / disabled / focused / selected` 等适用状态矩阵。
- 焦点顺序、键盘、鼠标、滚动、弹窗和高风险确认交互。
- resize、窄屏、SSH、低色彩、`NO_COLOR` 和字符能力降级。
- 页面局部决定、已批准例外，以及是否需要修改全局规范。
- 可直接转换为实现测试的验收条件。

一个页面只维护一个详情文件。页面级决定不得散落到额外的 `spec.md`、`states.md`、`interactions.md` 或独立字符帧文件中。

## 设计工作流

1. 读取 `AGENTS.md`、`kunpeng-devkit-ai-tui/docs/README.md`、相关研究文档、现行 `UI-SPEC.md` 和受影响页面文件。
2. 判断新决定属于全局规则还是页面局部规则。
3. 跨页面规则更新 `UI-SPEC.md`；页面详情更新对应的 `pages/<page-id>.md`，并引用全局规则 ID。
4. 在页面文件中列出本次全局规范影响和受影响页面，不再创建独立的 `system-impact.md`。
5. 新建或修改的交付先标记为 `draft`；人工审核通过后改为 `approved`。

## 正式交付就绪条件

- `UI-SPEC.md` 与目标页面文件的状态均为 `approved`。
- 页面 `ui-spec-version` 与全局版本完全相等。
- 所有规范性要求完整存在于 `UI-SPEC.md` 或目标页面文件中，不依赖研究文档、演示页面或图片补充关键语义。
- 页面引用的例外已经在全局例外登记表中批准。
- 布局、数据、状态、焦点、键鼠操作、终端降级和验收条件均无待猜测项。
- 任一文件缺失、仍为 `draft`、版本不相等、规则冲突、例外未登记或关键行为未定义时，交付视为未完成。

## 人工审核门槛

- 全局与页面规则归属是否正确，页面文件是否重复或覆盖全局规则。
- `UI-SPEC.md` 和页面声明的版本是否完全相等，状态是否均为 `approved`。
- 宽屏、窄屏、SSH、16 色、`NO_COLOR` 和纯键盘路径是否完整。
- 页面字符帧、状态、交互、数据和验收条件是否足够完整，后续实现无需猜测。
- 全局变更是否列出了所有受影响页面，并同步更新了这些页面的版本声明。
